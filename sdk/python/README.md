# VAOL Python SDK

Python client and instrumentation helpers for the Verifiable AI Output Ledger (VAOL).

## Install

```bash
pip install vaol
```

## Development

```bash
pip install -e ".[dev]"
pytest tests/ -v
```
